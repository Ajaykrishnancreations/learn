<!-- 

# Project Name (learn)
This project is created with TypeScript and is a sample README file for your reference.

## Getting Started
These instructions will help you set up and run the project on your local machine.

### Prerequisites
You need to have Node.js and Yarn (or npm) installed on your computer.

### Installing Dependencies
To install project dependencies, run:
```bash
yarn install
or
npm install

#Starting the Project
You can start the project with the following command:
yarn start
# or
npm start

Folder Structure
The project follows a specific folder structure:

(1)Src
    ApiService: API service for making HTTP requests
    Application: Components specific to the "Application" feature (e.g., login, home, etc.)
    Assets: Stores static asset images and files
    Components: Reusable components
    layout: Layout components like Header, Footer, SideNav, etc.
(2)App.tsx: Main application component (rendering all the components)
(3)index.tsx: Entry point of the application (id="root")

Naming Conventions
To maintain code consistency, follow these naming conventions:
Component Names: TitleCase or PascalCase (e.g., MyComponent)
CSS Class Names: snake_case (e.g., my-component)
Variable and Function Names: camelCase (e.g., myComponent) 

-->